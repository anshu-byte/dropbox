{
  "name": "dropbox",
  "version": "1.0.0",
  "description": "It is a simplified Dropbox-like service where users can upload, retrieve, and manage their files through a set of RESTful APIs. It supports the storage of metadata for each uploaded file, such as the file name, creation timestamp, and more.",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "start": "node src/setup/index.js && node src/index.js",
    "watch": "nodemon --watch src --ext js --exec 'node src/setup/index.js && node src/index.js'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-validator": "^7.2.0",
    "multer": "^1.4.5-lts.1",
    "node-cron": "^3.0.3",
    "nodemon": "^3.1.4",
    "pg": "^8.12.0",
    "uuid": "^10.0.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.9.1",
    "eslint": "^8.57.0",
    "eslint-config-airbnb-base": "^15.0.0",
    "eslint-plugin-import": "^2.30.0",
    "globals": "^15.9.0"
  }
}
